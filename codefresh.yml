version: '1.0'
stages:
  - prepare
  - build
steps:
  main_clone:
    title: Cloning main repository...
    stage: prepare
    type: git-clone
    repo: 'rainzha/cd-pipeline-sample-app'
    revision: multi-stage-docker
  BuildingDockerImage:
    title: Building Docker Image
    stage: build
    type: build
    image_name: spring-boot-2-sample-app
    working_directory: ./
    tag: 'multi-stage'
    dockerfile: Dockerfile